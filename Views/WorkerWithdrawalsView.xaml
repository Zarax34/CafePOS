<UserControl x:Class="CafePOS.Views.WorkerWithdrawalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             FontFamily="{StaticResource AppFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <DockPanel>
                <TextBlock Text="📊 سحبيات العمال"
                           FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrownBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"
                            VerticalAlignment="Center">
                    <TextBlock Text="من:" FontSize="13" Foreground="#555"
                               VerticalAlignment="Center" Margin="15,0,4,0"/>
                    <DatePicker x:Name="FromDate" Width="120" Height="36" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="إلى:" FontSize="13" Foreground="#555"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker x:Name="ToDate" Width="120" Height="36" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="تحديث"
                            Style="{StaticResource AccentButton}"
                            FontSize="14" Padding="16,8"
                            Click="Refresh_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="16" Padding="15" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
                </Border.Effect>
                <DockPanel>
                    <Border DockPanel.Dock="Bottom" Background="{StaticResource PrimaryBrownBrush}" CornerRadius="10"
                            Padding="12" Margin="0,10,0,0">
                        <TextBlock x:Name="TotalWithdrawalsText"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="White" HorizontalAlignment="Center"/>
                    </Border>

                    <TextBlock Text="العمال"
                               FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrownBrush}"
                               Margin="0,0,0,10"/>

                    <ListView x:Name="WorkerSummaryList"
                              BorderThickness="0" Background="Transparent"
                              SelectionChanged="WorkerSummaryList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" Margin="0,2" Background="#FAFAFA" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding WorkerName}"
                                                   FontSize="14" FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" FontSize="12" Foreground="#888"
                                                   VerticalAlignment="Center" Margin="8,0"
                                                   Text="{Binding WithdrawalCount, StringFormat='{}{0} عملية'}"/>
                                        <TextBlock Grid.Column="2" FontSize="14" FontWeight="Bold"
                                                   Foreground="{StaticResource ErrorRedBrush}"
                                                   VerticalAlignment="Center"
                                                   FlowDirection="LeftToRight"
                                                   Text="{Binding TotalAmount, StringFormat='{}{0:F2} ر.ي'}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="16" Padding="25" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
                </Border.Effect>
                <DockPanel>
                    <Border DockPanel.Dock="Bottom" Background="{StaticResource BackgroundBeigeBrush}" CornerRadius="10"
                            Padding="15" Margin="0,15,0,0">
                        <TextBlock x:Name="WorkerTotalText"
                                   Text="اختر عاملاً لعرض تفاصيل السحبيات"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrownBrush}"
                                   HorizontalAlignment="Center"/>
                    </Border>

                    <TextBlock x:Name="DetailTitle"
                               Text="تفاصيل السحبيات"
                               FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrownBrush}"
                               Margin="0,0,0,15"/>

                    <DataGrid x:Name="WithdrawalsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              BorderThickness="0" Background="Transparent"
                              RowHeaderWidth="0" SelectionMode="Single"
                              CanUserSortColumns="True" CanUserResizeColumns="True"
                              HeadersVisibility="Column"
                              FontSize="13"
                              AlternationCount="2"
                              GridLinesVisibility="None">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}"
                                                    Background="{TemplateBinding Background}"
                                                    BorderThickness="0">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BackgroundBeigeBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrownBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#F0EDEA"/>
                                <Setter Property="MinHeight" Value="42"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F2F0"/>
                                    </Trigger>
                                    <Trigger Property="AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="#FAFAFA"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="150"/>
                            <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat={}{0:F2}}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{StaticResource ErrorRedBrush}"/>
                                        <Setter Property="FlowDirection" Value="LeftToRight"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="الكاشير" Binding="{Binding CashierName}" Width="140"/>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
