<UserControl x:Class="CafePOS.Views.WorkersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             FontFamily="{StaticResource AppFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="👥 إدارة العمال"
                           FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrownBrush}"
                           Margin="0,0,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="اسم العامل" FontSize="12" Foreground="#888" Margin="0,0,0,4"/>
                        <TextBox x:Name="WorkerNameBox"
                                 Style="{StaticResource RoundedTextBox}"
                                 FontSize="14"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                        <TextBlock Text="رقم الهاتف" FontSize="12" Foreground="#888" Margin="0,0,0,4"/>
                        <TextBox x:Name="WorkerPhoneBox"
                                 Style="{StaticResource RoundedTextBox}"
                                 FontSize="14"/>
                    </StackPanel>
                    <Button Grid.Column="2" Content="إضافة عامل"
                            Style="{StaticResource PrimaryButton}"
                            FontSize="14" Height="42" Padding="25,0"
                            VerticalAlignment="Bottom"
                            Click="AddWorker_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <DockPanel>
                <Border DockPanel.Dock="Bottom" Background="{StaticResource PrimaryBrownBrush}" CornerRadius="10"
                        Padding="15" Margin="0,15,0,0">
                    <TextBlock x:Name="WorkersCountText"
                               Text="عدد العمال: 0" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center"/>
                </Border>

                <DataGrid x:Name="WorkersGrid" AutoGenerateColumns="False" IsReadOnly="True"
                          BorderThickness="0" Background="Transparent"
                          RowHeaderWidth="0" SelectionMode="Single"
                          CanUserSortColumns="True" CanUserResizeColumns="True"
                          HeadersVisibility="Column"
                          FontSize="13"
                          AlternationCount="2"
                          GridLinesVisibility="None"
                          Margin="0,0,0,5">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BackgroundBeigeBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrownBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#F0EDEA"/>
                            <Setter Property="MinHeight" Value="42"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F2F0"/>
                                </Trigger>
                                <Trigger Property="AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#FAFAFA"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="رقم الهاتف" Binding="{Binding Phone}" Width="200"/>
                        <DataGridTextColumn Header="تاريخ الإضافة" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150"/>
                        <DataGridTemplateColumn Header="" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✕" FontSize="12" Width="28" Height="28"
                                            Cursor="Hand" Tag="{Binding}"
                                            Click="DeleteWorker_Click"
                                            Background="{StaticResource ErrorRedBrush}"
                                            Foreground="White" BorderThickness="0"
                                            HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
